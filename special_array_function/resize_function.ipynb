#change the shape of an array
#if needed array is larger than the existing array,it creates duplicate values of exsiting value
arr9=np.array([[1,2,3],[4,5,6]])
np.resize(arr9,(1,6,2))    #resize(arr_name,size_needed)
