#single dimensiom(1D),such as 1D list 
#using vectorized math function we can apply a functions to an entire array without writing a loop
array=np.array([25.4,9.7,16.0])

#using built-in math functions
print(np.sqrt(array)) 
print(np.round(array))
print(np.ceil(array))
print(np.floor(array))
print(np.pi)
